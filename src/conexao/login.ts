
// import { db } from "../index"; // Importa a conexão com o banco de dados a partir do arquivo index.ts

// import * as bcrypt from 'bcrypt'; // Importa a biblioteca bcrypt para criptografar a senha



// export async function criarSenhaHash(senha: string): Promise<string> {
//     try {
//         const saltRounds = 10;
//         const senhaCriptografada = await bcrypt.hash(senha, saltRounds);
//         return senhaCriptografada;
//     } catch (error) {
//         console.error('Erro ao criar senha hash:', error);
//         throw error;
//     }
// }

